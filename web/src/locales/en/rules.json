{
  "title": "Routing Rules",
  "actions": {
    "newRule": "New rule",
    "editRule": "Edit rule",
    "createRule": "New rule"
  },
  "columns": {
    "priority": "Priority",
    "name": "Name",
    "match": "Match (condition)",
    "targetChannels": "Target channels",
    "enabled": "Enabled"
  },
  "form": {
    "name": "Name",
    "nameRequired": "Name is required",
    "priority": "Priority (lower matches first)",
    "enabled": "Enabled",
    "matchSectionTitle": "Match condition (empty field = unconstrained)",
    "groups": "Key groups",
    "groupsPlaceholder": "e.g. vip",
    "models": "Models (supports * wildcard)",
    "modelsPlaceholder": "e.g. gpt-4o, claude-*",
    "minTokens": "Min tokens",
    "maxTokens": "Max tokens",
    "expr": "Routing expression",
    "exprPlaceholder": "e.g. w == 1 && t > 500",
    "exprHelp": "Optional. Custom condition evaluated on top of the match above (both must pass).\nVariables: w (1=next turn likely writes/calls a tool, else 0), t (predicted output tokens), tokens (prompt tokens), group, model.\nExamples: w == 1 && t > 500   ·   group == \"vip\" || w == 1\nUsing w or t calls the small-model router classifier once per request.",
    "targetSectionTitle": "Target",
    "targetChannels": "Target channels",
    "targetChannelsPlaceholder": "Select existing channels",
    "targetGroup": "Or target group (optional alternative to channels)",
    "targetGroupPlaceholder": "channel group label"
  },
  "match": {
    "any": "any (no constraints)",
    "groups": "groups: {{value}}",
    "models": "models: {{value}}",
    "min": "min: {{value}}",
    "max": "max: {{value}}"
  },
  "target": {
    "none": "none",
    "group": "group: {{value}}"
  },
  "validation": {
    "targetRequired": "Select at least one target channel or a target group."
  },
  "popconfirm": {
    "deleteTitle": "Delete this rule?"
  },
  "toast": {
    "loadChannelsFailed": "Failed to load channels",
    "created": "Rule created",
    "updated": "Rule updated",
    "deleted": "Rule deleted",
    "enabled": "Rule enabled",
    "disabled": "Rule disabled",
    "saveFailed": "Save failed",
    "deleteFailed": "Delete failed",
    "updateFailed": "Update failed"
  }
}
